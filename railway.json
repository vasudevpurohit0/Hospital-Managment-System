{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "apps/api/Dockerfile"
  },
  "deploy": {
    "startCommand": "npx prisma migrate deploy && npx prisma migrate deploy --schema=prisma/platform/schema.prisma && npx prisma db seed && npx ts-node -r tsconfig-paths/register prisma/platform/seed.ts && node dist/main",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 180,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
